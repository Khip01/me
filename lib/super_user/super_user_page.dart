import 'package:flutter/material.dart';

class SuperUserPage extends StatelessWidget {
  const SuperUserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
